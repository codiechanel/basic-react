{
  "name": "basic-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "scripts": {
    "s": "simple-pack dev",
    "start": "simple-pack dev",
    "prebuild": "npx rimraf build/* && npx rimraf lambda/*",
    "b": "simple-pack prod",
    "build": "simple-pack prod",
    "build:lambda": "simple-pack build src/lambda",
    "sl": "simple-pack serve src/lambda",
    "start:lambda": "simple-pack serve src/lambda",
    "addglobal": "npm i -g file:/Users/admin/Desktop/myGithubProjects/simple-pack",
    "clean": "rimraf build/*"
  },
  "browserslist": {
    "development": [
      "last 2 chrome versions",
      "last 2 firefox versions",
      "last 2 edge versions"
    ],
    "production": [
      ">1%",
      "last 4 versions",
      "Firefox ESR",
      "not ie < 11"
    ]
  }
}
